nt BlackChess=1, WhiteChess=2;
int i, n, dirX, dirY, posX, posY;

chessboard[row][column] = chessColor;

for ( dirX=-1; dirX<2; dirX++)
	for ( dirY=-1; dirY<2; dirY++)
		if ( !( dirX==0 && dirY==0 ))
		{
			n=0;
			posX = row + dirX;
			posY = column + dirY;					
			while (posX<8 && posX>-1 && posY<8 && posY>-1 && chessboard[posX][posY] == (BlackChess + WhiteChess - chessColor))
			{
				posX +=  dirX;
				posY +=  dirY;
				n++;
			}
			if (posX<8 && posX>-1 && posY<8 && posY>-1 &&  chessboard[posX][posY] ==chessColor)
			{
				posX = row + dirX;
				posY = column + dirY;				
				for (i=0; i<n; i++)
				{
					chessboard[posX][posY] = chessColor;
					chessTurnPos[numPos][0]=posX;
					chessTurnPos[numPos][1]=posY;
					numPos++;
					posX +=  dirX;
					posY +=  dirY;
				}
			}
		}
